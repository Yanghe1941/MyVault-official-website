# 2026-05-26

## MyVault 官网更新

### 完成事项
- 替换 index.html 对比区域内容：将原有的"MyVault vs 其他应用"对比卡片替换为**免费版 vs Pro 版功能对比表格**
- 新增表格包含12个功能模块：物品管理、品牌管理、故事记忆、数字保险箱、千位分隔符、分类管理、资产图片分组、数据分析洞察、空间管理、场景功能、批量操作、iCloud 云同步
- 在 style.css 中添加定价表格样式（.pricing-table-wrap, .pricing-table 等）
- 表格支持移动端响应式设计

### 文件变更
- `/index.html` — 替换对比区域 HTML 结构
- `/assets/style.css` — 新增定价表格 CSS 样式

---

## MyVault 官网优化更新 (17:40-17:50)

### 已完成的优化

**P0 - Hero CTA 优化**
- 添加"跳过动画"按钮，点击立即显示完整标题
- TestFlight 按钮添加紧迫感标签（中文："名额有限"，英文："Limited spots"）

**P1 - 返回顶部按钮**
- 添加浮动返回顶部按钮，滚动超过 600px 后显示
- 点击平滑滚动回顶部

**P1 - 导航增强**
- 添加锚点导航链接（功能/对比/指南）
- 添加移动端汉堡菜单（768px 以下）
- 点击链接自动关闭移动菜单

**P1 - 深色/浅色主题切换**
- 添加主题切换按钮（太阳/月亮图标）
- 自动检测系统偏好（prefers-color-scheme）
- 用户选择保存在 localStorage
- 新增 [data-theme="light"] CSS 变量覆盖

**P2 - Schema.org 结构化数据**
- 中英文首页添加 SoftwareApplication JSON-LD
- 包含名称、描述、操作系统、评分等信息

**P2 - 性能优化**
- 添加 prefers-reduced-motion 媒体查询
- 动画减弱/关闭动画效果

**P2 - PWA 支持**
- 新增 `/manifest.json` PWA 清单文件
- 新增 `/sw.js` Service Worker（缓存策略）
- 中英文首页注册 Service Worker
- 添加 theme-color meta 标签

**P2 - 安全 Headers**
- 更新 `netlify.toml` 添加安全响应头
- X-Frame-Options: DENY
- X-Content-Type-Options: nosniff
- X-XSS-Protection
- Referrer-Policy
- Content-Security-Policy（允许 Product Hunt 等域名）

### 新增文件
- `/manifest.json` — PWA 清单
- `/sw.js` — Service Worker

### 修改文件
- `/index.html` (zh) — Hero CTA、导航、Schema.org、PWA
- `/en/index.html` — 同上
- `/assets/style.css` — 新增组件样式、主题变量、媒体查询
- `/netlify.toml` — 安全 Headers