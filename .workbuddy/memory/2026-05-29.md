# 2026-05-29

## 重新设计核心功能图表（index.html #features 区块）
- 将原 7 个功能图表替换为 6 个核心功能图表，对应新的功能体系：
  1. 资产录入与完整档案管理 — 表单卡片 + 字段分类 + 资产档案接收
  2. 资产列表、搜索与筛选 — 搜索栏 + 多维筛选芯片 + 资产列表行
  3. 资产详情与生命周期追踪 — 垂直时间线 + 状态卡片
  4. 分类化组织管理 — 6 维度放射状关系图（分类/品牌/空间/场景/标签/渠道）
  5. 资产分析与数据洞察 — 总值看板 + 趋势折线 + 饼图 + 保修/状态柱状
  6. 数据安全与掌控能力 — 层叠安全架构（Face ID → 本地存储 → 备份同步 → 零服务器）
- 每个图表均按用户提供的 6 段功能描述重新绘制 SVG
- Commit: 3de5d78，已 push 到 GitHub main 分支

## 精简核心功能区块（layout + 图表简化 + 删除对比模块）
- CSS 改动：
  - `.feature-chart-grid` → `grid-template-columns: repeat(4,1fr)`（4列布局，6模块=4+2）
  - `.feature-chart h3` → `font-size: 22px; white-space: nowrap`（标题单行不换行）
  - `.chart-wrap` → `min-height: 130px`（适配简化图表）
  - 响应式：≤900px 2列，≤560px 1列
- 6 个功能 SVG 全部重新绘制为单一焦点图表（viewBox 改为 400×100），每个模块只保留一个核心表达图：
  1. 表单 → 档案箱（录入→存档）
  2. 搜索栏 + 筛选标签 + 列表项（搜索筛选）
  3. 垂直时间线 + 持有时间卡（生命周期）
  4. 中心枢纽放射 6 维度（组织管理）
  5. KPI 卡片 + 趋势折线 + 柱状分布（数据洞察）
  6. 四层同心安全架构（数据安全）
- 删除 `#comparison` 整个对比模块（含 12 项功能卡、滚动按钮、图例、导航链接、JS）
- Commit: 90851e7，已 push 到 GitHub main 分支
